/**
 * Stub for static asset imports (images, fonts, etc.) in Jest tests.
 * Returns the filename string so tests can assert on file paths if needed.
 */
const fileMock = "test-file-stub";
export default fileMock;
