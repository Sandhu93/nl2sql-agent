[pytest]
addopts = -v --tb=short
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

markers =
    unit: Fully mocked — no external dependencies. Runs on host or in Docker.
    integration: Requires Docker stack (DB, Redis, ChromaDB). Run via docker compose exec.
